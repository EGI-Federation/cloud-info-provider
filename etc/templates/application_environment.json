{
  image_id: "${attributes["image_id"]}",
  % if "docker_id" in attributes:
  docker_id: "${attributes["docker_id"]}",
  % endif
  % if "docker_name" in attributes:
  docker_id: "${attributes["docker_name"]}",
  % endif
  % if "docker_tag" in attributes:
  docker_id: "${attributes["docker_tag"]}",
  % endif
  compute_service_name: "${attributes["compute_service_name"]}",
  suffix: "${attributes["suffix"]}",
  image_name: "${attributes["image_name"]}",
  image_version: "${attributes["image_version"]}",
  image_marketplace_id: "${attributes["image_marketplace_id"]}",
  image_description: "${attributes["image_description"]}"
}
